apiVersion: v1
kind: Service
metadata:
  name: timesheets-server
  labels:
    app: timesheets-server
    tier: frontend
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: timesheets-server
    tier: frontend
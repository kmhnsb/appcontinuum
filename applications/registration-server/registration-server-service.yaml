apiVersion: v1
kind: Service
metadata:
  name: registration-server
  labels:
    app: registration-server
    tier: frontend
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: registration-server
    tier: frontend
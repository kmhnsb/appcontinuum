apiVersion: v1
kind: Service
metadata:
  name: allocations-server
  labels:
    app: allocations-server
    tier: frontend
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: allocations-server
    tier: frontend